# Improvements: Liquity V1

## What Worked Well

- EVM typegen from Etherscan — direct contract, no proxy issues (Liquity V1 is immutable)
- SDK 1.0 `outputs` + `.pipe()` pattern — destructured multi-event decoding is clean
- Operation enum gives rich context without needing to cross-reference multiple contracts

## No Skills Patch Needed

Standard EVM indexer pattern — nothing new to document.
