Create a Yearn Finance V2 indexer that tracks StrategyReported events across ALL Yearn V2 vaults on Ethereum mainnet. Use topic0-only filtering (no contracts array) to capture harvest reports from every vault without needing to know addresses upfront. The StrategyReported event fires whenever a strategy reports gains/losses back to its vault, making it the heartbeat of Yearn's yield generation. Store vault address, strategy address, gain, loss, debtPaid, totalGain, totalLoss, totalDebt, debtAdded, and debtRatio. Start from block 11000000 (Yearn V2 launch era). Dashboard should show harvest frequency over time, top vaults by activity, gain vs loss trends, and strategy activity distribution.
