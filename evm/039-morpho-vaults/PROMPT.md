Create a MetaMorpho vault curator reallocation indexer on Ethereum. Track ReallocateSupply and ReallocateWithdraw events across ALL MetaMorpho vaults using topic0-only filtering (no contracts array). These events fire when curators shift capital between Morpho Blue markets. Capture vault address, caller (allocator), market ID, assets, and shares. Dashboard should show weekly reallocation activity, top vaults, top markets, and top allocators.
