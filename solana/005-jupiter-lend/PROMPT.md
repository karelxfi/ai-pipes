Create a Solana indexer for Jupiter Lend that tracks liquidity operations on the core Liquidity program (jupeiUmn818Jg1ekPURTpr4mFo29p46vygyykFJ3wZC). Index the unified `operate` instruction which handles supply, withdraw, borrow, and payback via signed i128 amounts. Use @subsquid/solana-typegen with the GitHub IDL to generate typed decoders. Classify operations by sign of supply_amount and borrow_amount. Must enable innerInstructions since all operate calls are CPI from the Lending/Vaults programs. Dashboard should show hourly ops by type, top assets, operation type distribution, and hourly total activity.
